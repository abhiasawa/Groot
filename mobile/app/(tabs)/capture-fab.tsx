import React from "react";
import { View } from "react-native";

// Placeholder — this screen is never actually rendered.
// The tab press is intercepted to open the compose modal.
export default function CaptureFabPlaceholder() {
  return <View />;
}
